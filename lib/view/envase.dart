import 'package:flutter/material.dart';

class EnvasePage extends StatefulWidget {
  @override
  _EnvasePageState createState() => _EnvasePageState();
}

class _EnvasePageState extends State<EnvasePage> {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('envase'),
      ),
    );
  }
}